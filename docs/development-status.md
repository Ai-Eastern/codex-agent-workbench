# 新版开发进度

规则来源：[DEVELOPMENT.md](../DEVELOPMENT.md)，版本 v1.2，2026-09-21。本页只记录实施与证据，不另立开发规则。

当前状态：新版控制器、三项目业务夹具及安装/CI材料已实现，正在发布实现分支。**真实 A/B/C Desktop 验收尚未运行，因此不是已完成的功能版本。** 未运行付费模型对照、未修改个人全局配置、未整体合并实验 PR #1。

| 任务 | 已实施范围 | 验证边界与下一动作 |
| --- | --- | --- |
| WB-00 | 独立克隆、原型测试基线、[宿主能力矩阵](host-capabilities.md) | 基线 155 项中 154 通过、1 平台跳过；真实独立 Desktop 绑定/等待/接续待验收 |
| WB-01 | 项目计划/总览/动作/回执合同，显式模型配置，版本化产物声明 | 合同反例与旧请求兼容测试通过；配置不证明实际服务模型 |
| WB-02 | Markdown 单计划块、不可变快照、expectedRevision 原子激活 | 并发发布只允许一个成功，半写不激活，保留旧任务包 |
| WB-03 | 显式项目绑定、范围校验、私有总览、增量事件与过期引用识别 | 同名任务/文件/知识的项目隔离测试通过；真实项目仍需登记 |
| WB-04 | 每阶段组复用旧 run，冻结输入、三项调度决定、下一动作 | 继续使用 direct/native/Desktop 和原失败恢复；重复准备不重复派工 |
| WB-05 | 事实交接包、新负责人身份链、原配置/attempt/失败保留 | 协议夹具通过；真实新上下文创建与宿主来源需另验 |
| WB-06 | 局部影响传播、旧结果拒绝、在途冻结、顺序写集转移 | 未派出旧组可凭本机完整证据显式退出；已派出或状态不明不释放 |
| WB-07 | 全局原子额度、attempt 预算、公平队列、项目/全局暂停、代次 | 组合测试覆盖无额度拒派与暂停期间收集；确认完成/从未派出才归还 |
| WB-08 | [A/B/C 自包含夹具、冻结验收与证据校验器](three-project-demo.md) | 业务基线含预期失败，参考补丁仅验证夹具；真实三项目案例未执行 |
| WB-09 | [12 题探索性协议](evaluation/pilot-12-protocol.md) | 评测仓库与运行材料待建，0 次付费对照，没有提效结论 |
| WB-10 | [安装说明](installation.md)、双语能力表、Windows/Linux 确定性 CI | 隔离安装已验证；远端 CI 与真实宿主门槛分别记录，不提前打功能发布标签 |

## 本轮集成记录

- 日期：2026-09-21。
- 起点：`9b08a8b9f90c0eb2773b79ea11f4a46afe8b6212`；同步了主页提交 `ce47350e487faef78a8969e2b9c194ac5f8612ba`，保留英文默认、中文切换及英文图片。
- 复用：现有 `SqliteSaver`、LangGraph 四阶段、runs/tasks/events/ownership、知识索引、回执和恢复入口。新增合同不依赖 LangGraph 内部类型，没有第二套模型执行器或独立前端。
- 已修复审阅反例：新版生效后旧准备组仍能启动、最终产物漂移被报完成、准备恢复漏查依赖输入、任务包半写后无法接续。四个回归先失败后通过，加入 `test/phase-execution.test.mjs`。
- 交接集成修复：事务内复用同一数据库连接；新负责人提示使用另存的新配置，冻结旧任务包和旧配置保持不变。
- WB-01–08 控制器与夹具候选验证：2026-09-21 19:54（Asia/Shanghai），`npm test` 共 249 项，248 通过、0 失败、1 平台跳过，耗时 22,847 ms；`node src/cli.mjs --help` 与 `git diff --check` 通过。该记录不含仍在独立补充的三业务仓库组合测试，也不作为真实宿主通过声明。

## 仍需完成

1. 在明确授权或手动登记后，配置 A/B/C 三个真实独立项目任务和 B 的新上下文，执行规范第 19 节。现有宿主工具只允许在用户明确要求时创建额外用户可见任务。
2. 保存真实发送/等待、计划变化、暂停/恢复、原始宿主来源、代码提交和完整验收证据。不可观测模型与用量字段按缺失项记录。
3. 在实际证据范围内完成 WB-08 与 WB-10 功能发布门槛。取消已派出任务、未明送达、失败后的额度调整继续受明确对账约束，不凭超时释放；全局总调度身份迁移和额外执行扩组未声明支持。
4. 获得单独冻结预算后再启动 WB-09 对照；普通功能验证不自动扩展成批量评测。

真实 UUID、本机路径、原始对话、凭据和私有运行记录仅保留本机。历史报告仍只适用于其明确提交和环境。
