# 正常交付观察：取消重复派工报告之后

本轮已在真实任务中观察到新版交接规则生效：总经理使用原生回执完成派工，未再手写重复报告；PM 完成一次正式验收和一次知识保存，生成交付回执后结束。整体交付成本没有下降，尚不能宣称框架实现整体提效。

框架基线为 `be180e49743333a1689f756e228345aa44f2bdf2`。本轮没有修改框架代码或 Skill、增加 Ruflo 模块，也没有开启新桌面任务或并行测试。沿用现有 GM Astra/ultra 与恋语 PM Sol/high；PM 按依赖选择 direct、0 工程师。

## 本轮产品交付

根据已有数据准备规划，把已经验真的 6 条正向训练种子与 6 条独立反例整理为 **12 条人工复核记录**。工作包保留策略、原文及来源哈希，并提供关系阶段、投入度、情绪、意图、风险和审核决定等人工字段；全部保持 PENDING/null/空数组，不生成新回复、不填造人工结论。

控制器 COMPLETE，正式集成验收 **1 次，8 类检查通过**：12 条与 6+6 数量、待复核字段、策略及来源一致、正负隔离、确定性输出、输入哈希不变、非空输出保护、坏输入拒绝。没有失败的正式验收、修复 attempt 或第二次正式验收。

开发自测记录保留：先因实现文件尚未创建而失败；实现后完整检查通过；同一检查随后在控制器正式验收中执行一次并通过。因此不能写成所有命令首次通过，或整套检查只执行过一次。人工复核和模型训练均未开展，证据级别为本地命令。

PM 调用一次项目 RAG 检索，返回 5 条知识，其中包含之前保存的“显式标注目标回复、隔离反例”规则；交付后新增 1 条知识到项目 Obsidian 目录。默认仍是 FTS5/BM25。这证明本轮实际检索和保存，不是检索准确率或知识防错效果对照。

## 管理步骤的前后变化

| 指标 | 上轮：只读验真 CLI | 本轮：人工复核工作包 |
|---|---:|---:|
| GM 手写重复派工报告 | 1 | **0** |
| GM 顶层工具调用 | 4 | **2** |
| GM 模型响应 | 5 | **3** |
| GM 完整轮次 | 113.913 秒 | **69.706 秒** |
| GM 原始总 Token，含缓存 | 583,147 | 369,273 |
| GM 非缓存输入 | 120,474 | 124,488 |
| PM 正式验收 | 1 | 1 |
| PM 知识保存 | 1 | 1 |

当前 GM 两次顶层调用分别读取交接依据与派送，发送回执之后没有工具调用；PM 最后一次工具调用就是带输出路径的 continue，没有完成后额外 status 或 delivery。统计方从原生发送回执核对身份，不依赖手写派工文件。

顶层调用是 exec 包装层数量，不是内部工具总数。调用减少同时涉及读取合并与删除重复报告；不能把全部差额归因于单项改动。GM 非缓存输入略增，也不能把原始总 Token 的减少直接等同于订阅费用下降。当前回执到 GM 结束约 3.786 秒；上轮重复文件保存区间与 PM 执行重叠，不能据此相减得出端到端收益。

## 耗时仍集中在哪里

| 本轮区间 | 耗时 |
|---|---:|
| PM 完整轮次 | **536.764 秒，约 8分57秒** |
| PM 开始至控制器 PREPARED | **279.038 秒，约 4分39秒** |
| PREPARED 至启动 | 5.653 秒 |
| RUNNING：执行与等待 | 245.374 秒 |
| 控制器正式验收命令 | 0.082 秒 |
| 控制器记录总窗口 | 251.154 秒 |
| 控制器完成至 PM 结束 | 6.572 秒 |

准备前的实际调用包含需求筛选、RAG、规划与规则读取、来源结构检查和合同准备。这一段接近 PM 轮次的一半，是下一项真实需求值得关注的范围；其中包含必要设计工作，不能整体标为无效调度开销。执行区间仍未拆成纯编码、模型计算、工具与等待。没有重复正式验收的证据，也不应把优化重点放在本轮仅 82 毫秒的验收命令上。

PM 本轮有 18 次顶层工具调用、19 次模型响应，工具返回共 107,846 个字符；字符量不能当作 Token，当前证据也不能逐工具归因 Token。

## 成本分账

| 角色/用途 | 原始总 Token，含缓存 | 非缓存输入 | 输出 |
|---|---:|---:|---:|
| GM 管理派工 | 369,273 | 124,488 | 1,585 |
| PM 产品交付 | 3,571,146 | 211,451 | 22,991 |
| **产品＋管理** | **3,940,419** | **335,939** | **24,576** |
| 维护者协调与观察，截点下界 | 1,809,556 | 65,956 | 18,800 |

采集一次，截点为 **2026-09-12 14:13:59.922 UTC**。GM 和 PM 的指定轮次完整；维护者尚未结束，后续整理报告和发布未计入，不能当作本轮全部开销。本轮维护成本属于协调、观察与统计，不是新的框架实现成本；维护者截点已有 23 次顶层工具调用，观察本身仍有成本。

缓存输入是输入的子集，不重复相加，不换算 Codex 订阅账单。上轮产品＋管理为 2,893,466 原始 Token、263,887 非缓存输入，PM 约 7分31秒；本轮均更高。业务内容、既有上下文与准备工作不同，不能据此推导框架更快或更慢的因果结论。

## 后续使用方式

保留已经验证的简短派工、按需分流、一次正式验收和知识保存。下一项真实开发在派工时尽量给出明确的业务需求与最新交付入口，减少接手者重新寻找“应该做什么”；必要资料仍按任务读取。本轮不再追加证明同一结论的测试或增加框架模块，只有实际阻塞或反复出现的问题才进入修复。

原始回执、明确轮次范围、工具记录、成本快照与收集脚本保存在本机 `.local/normal-delivery-20260912`，未进入 Git。仓库只发布本报告及[匿名结构化结果](normal-delivery-results-20260912.json)。统计核对了验收、全部交付产物和新知识的哈希；没有重跑产品验收或框架测试。[上轮记录](task-timing-results-20260912.md)保留原始事实。
