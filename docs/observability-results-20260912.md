# Observability 执行链路分析结果

已增加按需只读的 `trace-report`，复用既有控制器事件、最后一份绑定验收回执和可选成本报告。它能分清已记录的状态耗时，并明确哪些耗时仍未知。安装版 Skill 的成本说明同步更新；普通开发不增加固定报告轮，也没有新增模型调用。

采用 [Borrow 决定](observability-reuse-20260912.md)，借鉴 Ruflo 固定版本的时间轴与瓶颈解释方式。没有安装外部追踪服务或导入其模型分析入口。实现见 [trace-report.mjs](../src/trace-report.mjs)，入口见 [cost.md](../skills/codex-project-workbench/references/cost.md)。

## 恋语历史任务的实际观察

使用原项目配置读取已完成的首批训练数据 CLI 任务。读取前后的 run 状态、验收摘要哈希、暂停标记、任务状态/attempt、事件计数和末事件 ID 均一致。没有重新派工或执行该项目的验收。

| 控制器记录区间 | 耗时 |
|---|---:|
| 准备记录建立后待启动 | 5.610 秒 |
| RUNNING：执行与等待，尚未拆分 | 124.005 秒 |
| ACCEPTING：验收状态 | 0.982 秒 |
| ACCEPTED：验收后知识处理 | 0.049 秒 |
| 已完成记录总窗口 | **130.646 秒** |

最后一份验收回执通过哈希和 run/request 身份绑定，命令 exitCode 为 0，记录耗时 **0.979 秒**。这与验收状态的 0.982 秒是不同测量范围；没有用历史回执重新证明当前文件仍通过。该样本的验收占时很小，优先待定位的是 124.005 秒内的执行与等待。不能把整段 RUNNING 称为纯编码、模型计算或调度浪费。

单独附加的旧成本快照覆盖总经理、PM 及框架维护 Agent，范围大于该 run。其 PM 阶段 turn 记录 **250.528 秒**；它只是独立报告的阶段时长，工具不按角色名或时间重叠自动绑定到 run，也不将两层相减命名为管理开销。快照中的维护者尚未结束，因此补充层为部分观测。

跨任务时钟未核实，合并墙钟、并发峰值与关键路径均返回未知。即便单任务区间可计算并集，turn 开始仍可能是首次 context/usage 记录，包含工具与等待。暂停缺少恢复时间，历史任务回执缺少足够的 attempt 绑定和工程师开工事件，均不补造耗时。可选快照只做结构及时间字段校验，本次 trace 入口未对原会话日志重新验真。

## 验证与修正

- 新增 6 项测试，覆盖状态分段、重复状态、暂停/未结束区间、倒退时钟、缺失或重复 turn、跨任务时钟边界、回执哈希失效、配置身份变化、只读观察和禁止覆盖报告。
- 首次 fixture 清理在 Windows 因 SQLite 未关闭而失败；修正 hook 顺序后定向测试 6/6 通过。失败记录保留，没有把清理异常删除后当作首次成功。
- 独立 Luna/medium 审查指出跨任务合并区间依赖时钟一致性，以及成本快照的 complete 不代表重新验真。最终实现保留未知与来源声明；最初的私有观察输出保留，旧合并值不进入正式结果。
- 最终完整命令 `node --test test/*.test.mjs`：**122 项，121 通过、0 失败、1 项既有平台相关跳过**，约 3.60 秒。这是自动化测试耗时。
- 安装版 cost 引用与源码哈希一致，旧引用已备份，runtime 保持不变；Skill validator 通过。

原始观察、成本 manifest、失败记录和完整测试输出保留在本机 `.local/observability-20260912`，不提交真实配置、任务 ID 或聊天日志。匿名数据及源码哈希见 [JSON 报告](observability-results-20260912.json)。

## 本次建设消耗

复用现有成本采集器，明确限定维护者当前 turn 与一名独立审查 Agent 的两个 turn。截点 **2026-09-12 12:29:42 UTC**；维护者尚未结束，未包含其后报告与发布工作，下表为已观测下界。

| 用途 | 原始总 Token（含缓存） | 非缓存输入 | 输出 |
|---|---:|---:|---:|
| 实现与验证，未结束 | 2,483,882 | 106,922 | 25,472 |
| 独立时间契约与代码审查 | 556,786 | 118,001 | 5,889 |
| 合计 | **3,040,668** | **224,923** | **31,361** |

缓存输入 2,784,384 已包含在输入中，不重复相加，不换算订阅美元费用。此建设成本仍高；分析入口运行时无需模型，不能据此抹掉建设消耗或声称整体节省。与其它模块建设消耗不同也不构成公平效率对照。

下一步应在正常真实交付中使用已有入口，优先补足执行与等待的最小时间边界和明确的 run/task/attempt 关联。仅在这能回答具体瓶颈问题时增加记录；沿用一次集成验收，不再为报告额外派一轮工程师。当前没有实测的整体提速、错误率或返工率下降比例；LearningBridge 整体接入仍暂缓。
