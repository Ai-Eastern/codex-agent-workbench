# 贡献说明（发布准备）

当前原始代码仍使用保留所有权利的许可证。维护者需先确定开放源码许可证及贡献条款，再正式开放外部贡献；本页不额外授予使用或再许可权利。

## 本地验证

使用 Node.js 24+，运行 `npm ci`、`npm test` 和 `npm run evaluate -- --output .local/contribution-check`。输出目录应全新；如需再次执行，选新目录保留前次证据。演示使用公开 fixtures，不需要个人任务身份、API Key 或外部服务。

修改应复用现有状态、检索和测试入口。新增状态/恢复行为时，覆盖失败、重复调用及旧证据失效；文案与样式改动无需添加镜像式测试。工作台 UI 使用原生 HTML/CSS/JS，通过 `npm start -- --repo <目标仓库> --state <仓库外状态目录>` 验证桌面与窄屏、键盘操作、任务提交、停止、失败修复及 diff。提交任务会使用现有 Codex 登录和额度；自动化测试用测试执行器验证协议，不能替代真实模型验收。`npm run demo` 是可选的机制报告查看器。

## 提供可核验的问题描述

描述预期行为、实际行为、最小复现、Node/操作系统版本和源码版本。共享前检查报告内容；真实项目日志可能包含本机路径、业务内容或身份。不要提交个人知识库、凭证、原生聊天记录、客户仓库和私有运行配置。

说明测试命令与观察结果；区分本地自动化、真实模型运行、GUI 检查和人工验收。确定性 fixture 的成功不计入模型 benchmark。保留失败记录，不挑选最好的一次运行当作全部结果。

已知边界见 [证据地图](docs/evidence-map.md)。涉及安全问题时，先联系仓库维护者使用双方认可的私密渠道；不要把密钥或可识别的客户资料写入公开 issue。
