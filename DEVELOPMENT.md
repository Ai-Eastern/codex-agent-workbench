# Codex Agent Workbench 开发规范：多项目 Agent 编排框架与 Coding Agent 工作台

版本：开发规范 v1.2｜日期：2026-09-21｜状态：方向与开发已授权，新增能力待实施和验证

本规范以原型 8c85c7672f5e289ba7ca72b5d7ea4e6ae9efdd17 为实现基线，另参考实验分支 1a23e19d61e50a4f145a91a4b502d58cacf90c25 中可单独复用的修复。本文是新版实现的唯一开发规则来源；现有运行能力见 README 和对应提交的验证记录。本文新增接口、里程碑与案例均是开发要求，不能当作已完成能力。执行进度维护在 docs/development-status.md，不在多份文档重复维护规则。

接续开发者先读本文第 1、10、13、17–20 节，再检查相关源码与测试。按 WB-00 开始实际开发；不要重新制作展示页面、另写一套替代方案，或仅回复“已阅读，等待指示”。具体任务开始前读取其依赖章节。

**产品定义**

一个仓库内包含可复用的 Agent 编排核心，以及使用该核心的 Coding Agent 工作台，Codex 是第一执行宿主。用户在一个总调度会话中与强能力主 Agent 沟通，由它统筹多个项目的目标、优先级、跨项目依赖和资源。每个项目拥有独立主会话、开发文档、执行状态与知识范围，按阶段选择串并行和原生子智能体，通过受控交接推进。程序负责状态、约束、证据、资源和接续，模型负责理解、规划、实现与必要的重新判断。

框架核心提供项目、计划、任务、调度与交接协议；Coding Agent 工作台将它们用于仓库改动、测试和交付；宿主适配负责真实会话与工具调用。先做通具体开发流程，再提炼已经重复出现的机制，不预建多宿主插件平台，不要求拆成多个包或仓库。分层与委派本身属于已有模式，工程差异化需要由可复用协议和真实对照支撑。

首批使用场景：用户已有 Codex 使用习惯，同时推进多个已有项目，各项目包含跨文件/模块、分阶段和可能变化的修复、小功能与局部重构。用户只维护一个总调度沟通入口，开发细节留在各项目会话。技术同行通过可复现案例理解工程价值。

成功标准：减少用户传话、重新解释上下文、重复开发和核对交付的负担，同时保持验收质量，并把新增协调成本计入结果。

## 1. 固定产品边界

1. 用户入口保留在 Codex；命令行是 Skill 调用的内部控制接口。
2. 保留 direct、原生子智能体、独立 Desktop 会话三种实际执行方式。
3. 串并行关系、执行载体和上下文安排分别决策；LangGraph 暂时保留为内部流程与检查点组件，不等于独立会话，也不成为框架核心协议的必选类型依赖。
4. 单个主会话内部，原生子智能体上限为 3。这是本项目策略，不是宣称所有 Codex 版本的固定上限。
5. 项目独立会话用于保留各自上下文，不受“4 项工作”门槛限制。为了增加执行并发而额外扩组，才要求当前确实需要至少 4 项独立并发工作，并通过收益、资源和预算检查。
6. 1-2-6、1-3-9、1-4-12 只是展示用的协调者/负责人/执行者容量记号，不是调度输入。协议逐一记录 projectId、coordinatorId、parentCoordinatorId、groupId 与 executorId，区分跨项目负责人和单项目额外执行组。独立主会话不等于根 Agent 的原生子智能体；实际规模仍服从宿主和账户容量。单项目额外扩组沿用成本门槛，不默认再叠一层管理。
7. 每个阶段先有目标、接口、依赖、调度策略与验收；细节变化时局部修订。
8. 保留实际 Codex 身份、失败记录、授权范围和原项目入口；不能用伪造身份或另起隐藏 headless 执行器代替 Desktop 协作。
9. 首版不新增 Web 产品、独立聊天前端、多模型平台或常驻跨项目后台服务。
10. 合并、发布等用户边界沿用项目约定；普通调度与范围内返修不引入重复确认。

## 2. 用户体验

用户在总调度会话说明目标，例如：“同时推进工单系统的批量导入、知识库项目的检索改造和 SDK 的兼容修复；工单系统优先，SDK 改动不能破坏现有调用。”总调度将各目标路由到绑定项目，并统一呈现进度与需要用户决定的事项。

总调度先确定项目归属、总体目标、优先级和资源预算，各项目主 Agent 再读取自己的仓库与文档形成项目计划。缺失的业务问题统一经总调度向用户询问。获得开发授权后，在既定范围和预算内执行。用户不需要决定哪个 Agent 接哪个文件，也不需要在窗口之间复制交接材料。

主会话向用户报告：

- 初始开发计划及关键假设。
- 里程碑完成与可检查的代码结果。
- 影响交付目标、范围、预算或外部行为的重要变更。
- 当前无法自行解决的阻塞。

常规派工、结果收集、验收和局部调整记录在控制器中，不逐项打断用户。用户可以随时说“进度如何”“为什么改成串行”“暂停”“这个约束改一下”；主 Agent 从同一份计划与状态回答。

暂停的产品语义必须明确：立即停止新派工；已有执行者进入待收尾/待停止状态，只有收到宿主终止证据才显示已停止。暂停后不得由自动接续机制再次启动。

**多项目总调度的上下文与职责**

总调度使用用户明确选定、当前账户可用的最强能力模型配置，承担跨项目取舍、冲突与复杂异常判断。项目主 Agent 和执行者使用明确配置，不暗中降级。程序生成状态、校验依赖和等待事件，避免强模型反复做机械汇总。

每个项目向总调度提供一个有版本的简明状态：projectId、目标、当前阶段、计划版本、关键进度、阻塞、资源占用、下一动作、交付证据引用和更新时间。普通日志、完整源码、探索过程和知识正文留在项目内。总调度仅在解决具体跨项目问题时按需取得必要证据。

项目状态在控制库中保留当前快照与事件历史；新汇报优先返回自上次读取以来的变化，旧快照不因每次轮询而重复进入模型。已经进入聊天的信息不会因为文件被更新而自动消失，因此仍需压缩后的受控恢复或必要的真实新会话交接。

总览计划只维护项目目标、优先级、跨项目依赖、资源预算和项目计划引用；每项目 DEVELOPMENT.md 保持自己的执行规则单源，不能在总览中复制一套会漂移的任务清单。

所有派工、结果、知识检索都绑定 projectId 和对应身份；含糊的“这个项目”在无法由当前指令和焦点唯一确定时向用户澄清。跨项目依赖通过明确的接口、产物版本和来源连接；知识迁移由显式的共享条目完成，不默认全库互搜。

资源分配同时考虑用户优先级、可并行性、关键依赖和项目等待时间，防止一个项目无限占满额度。独立项目自行推进；只有存在真实跨项目依赖或用户要求统一启动时才使用同步屏障。这里“同步推进”表示多个项目在同一时期持续取得进展，不要求所有项目同时开工或等最慢项目。

上下文隔离和并发扩容分开计算：两个项目各只有一项工作，仍各自保留项目主会话，可以由各自主 Agent 直接完成，不强行启动六个工程师。新增项目分会话需要同时满足真实宿主能力和明确用户授权；文档、Skill 或模型计划不能替宿主授予创建权限。只允许已登记会话的环境标注为“手动创建后绑定”，不假称自动创建。

可交付目标是减少执行噪声和项目约束串用，并通过版本和身份校验发现错误。不能承诺模型绝不混淆，也不能把同一 Desktop 账户内的独立会话说成严格保密边界。

## 3. 开发文档与执行计划

**采用一份可编辑计划源，生成执行快照。**

建议在项目约定的文档目录维护 DEVELOPMENT.md。正文解释业务目标、架构选择和理由；一个标记明确的 JSON 计划块承载可执行字段。控制器校验后，将对应版本保存为不可变快照。它复用现有 JSON 合同验证，不引入自定义语言或第二套手工维护的 YAML 规则。

多项目再增加一份轻量的总览计划，保存项目引用、优先级、明确的跨项目交付关系和全局预算。它不重复各项目实施细节，项目计划变更通过引用版本反映到总览。

如果仓库已有等价开发文档，沿用原位置。文档中的普通文本、引用资料和代码片段不直接成为命令或授权。只有通过 schema 和权限校验的计划字段进入调度。

计划包括：

| 层次 | 必须包含 |
| --- | --- |
| 开发目标 | 范围、非目标、验收标准、用户明确约束、预算 |
| 阶段 | 阶段目标、前置阶段、输入与交付物、阶段验收、候选调度方式及理由 |
| 任务 | 稳定 taskId、目标、依赖、文件归属、共享接口、执行载体、验收、上下文引用 |
| 调度 | 同时活跃人数上限、组划分、是否允许创建新会话、扩缩容条件 |
| 变更 | 基于哪版、变更原因、受影响任务、兼容性、谁决策、何时生效 |

前期覆盖全部阶段和跨模块接口，避免边做边猜。精确文件和命令在该阶段派工前核对；远期细节允许按证据完善，避免在开工前为所有未来文件反复规划。

任务状态不让模型手写回文档。完成情况、attempt 和实际派工记录由 SQLite 管理，再生成可读进度摘要。项目知识单独存储，不能代替执行状态。

**版本关系**

- objectiveId：整个用户开发目标。
- planRevision + planHash：生效的计划版本。
- phaseId / groupId：阶段与组。
- runId：继续复用当前控制器的一次运行。
- taskId / taskRevision / attemptId：任务定义与实际尝试。
- inputArtifactHashes、contextHash、acceptanceHash：本次执行的输入和验收依据。

发布计划使用 expectedRevision 比较和数据库事务；发现别人已更新则重新读取并合并。文件先写不可变快照，再在事务中切换 active revision；中途失败留下未激活文件，不让半份计划生效。

每个 attempt 在开始时冻结上述引用。更新计划不会修改运行中任务包。人工直接编辑文档也只形成待校验修订，不能偷偷改变已经派发的合同。

## 4. 模型如何选择调度

主 Agent 在阶段准备和重要变化时，分别提出三个判断：

1. **依赖拓扑：** 哪些工作串行、并行，哪些共享接口须先确定。
2. **执行载体：** 当前主会话、短期原生子智能体、需要持续负责的独立组主会话。
3. **上下文安排：** 延续、按任务选取必要资料，或在安全交接点使用新上下文。

判断输入包括依赖、写集交叉、预计工作量、交接需求、当前上下文中的可观测问题、可用会话、实际能力和剩余预算。模型输出选择、理由和关键不确定性；控制器只校验可执行性，不再调用另一个模型完整重做规划。

| 当前就绪且适合同时执行的独立工作 | 候选容量 |
| --- | --- |
| 1 | 当前主 Agent 或 1 个必要执行者 |
| 2–3 | 单主会话，至多 3 个原生执行者 |
| 4–6 | 单会话分批与 1-2-6 比较 |
| 7–9 | 分批、小分组与 1-3-9 比较 |
| 10–12 | 分批、小分组与 1-4-12 比较 |
| 更多 | 优先继续分阶段/分批，不增加组织层级 |

“就绪”指前置交付已经核对可用，写集和运行资源不冲突；不是整个项目的任务总数。模型可以提前安排下一阶段的拓扑，但实际派工仍须满足依赖。

上表是执行扩容的候选规则，不决定项目会话是否应该独立。在多项目模式中，还须先分配全局额度，再由各项目在分到的额度内决定串并行；分配不等于要求把额度用满。

只有预期缩短的交付时间值得额外的规划、创建会话、装载上下文、交接和集成开销时才扩容。缺乏估计证据时采用较小规模，记录估计与实际差异供后续调整，不编造精确收益。

调度人数是上限，不凑角色。四项工作可以分成两组、每组两个执行者。工作变少后停止补发，已有尝试完成并交接后收缩。正常阶段保持选定策略，避免在每条工具结果后反复改变组织。

首版把阶段切换作为主要决策点；阶段内发生实质依赖或资源变化时才局部重排，不人为设置频繁的模型复核轮次。

## 5. 主 Agent、组主 Agent 和执行者

| 角色 | 负责 | 输出 |
| --- | --- | --- |
| 用户主 Agent / 总调度 | 理解用户目标、项目优先级、跨项目依赖、全局预算与异常、汇总交付 | 总览修订、跨项目决定、用户摘要 |
| 项目主 Agent / 必要的组主会话 | 维护项目开发计划；在授权范围内安排至多 3 个执行者，推进本项目阶段并验收 | 项目交付、阻塞、资源请求或跨项目变更建议 |
| 原生执行者 | 完成一个有界任务，遵守文件归属，必要自测 | 产物、失败事实、下一步与回执 |
| 控制器 | 身份、版本、依赖、资源、状态、验收证据、重复请求防护 | nextAction、进度、交接与交付事实包 |

单主会话场景不另设总协调角色。组主不重写全局方案，执行者不递归扩张团队。上下层约定模块接口之后，组内的实现细节由组主负责；影响其他组时只提交一份明确的变更建议。

多项目模式中，总调度不重新验收每个项目的全部细节，只核对项目交付事实和跨项目兼容性；项目内部常规变化由项目主 Agent 处理，涉及跨项目接口、全局资源或用户目标时再上报。

所有人均可按需读取有效项目知识；不能让每层重复全仓检索和总结相同资料。总结由已有事实包生成，同一证据不为逐级汇报反复验收。

## 6. 上下文治理与交接

**减少噪声进入，必要时更换执行上下文，两者同时做。**

主 Agent 保持目标、约束、计划版本、决定和结果摘要；长日志、探索分支和源码细节留在执行者或文件中。既有 FTS5/BM25 按任务检索知识，返回来源引用；不能每次加载整个知识库。

这项规则同时作用于两层：总调度只保留跨项目决策信息，项目主 Agent 只保留本项目决策信息。默认不将总调度混合了多个项目的整段对话传给项目 Agent，也不将 A 项目的失败现场传给 B 项目。

交接包由模型提供必要解释、控制器绑定事实，至少包含：

- 当前目标、计划/任务版本与权限范围。
- 仓库基线、工作区、产物路径和哈希。
- 已完成事项与实际验证证据。
- 未完成事项、失败尝试及其证据。
- 已确认的接口、有效假设和已失效假设。
- 下一步、允许动作、剩余预算。
- 必要知识 ID、来源与完整详情位置。

交接包不复制全部聊天。长度预算作用于解释和噪声，不能为缩短文本删去用户约束、失败事实或未决问题；信息过多时保留摘要及可按需读取的引用。

新上下文启动后，由控制器核对输入版本与实际代码，由模型处理剩余业务理解。无冲突时这项接收检查直接并入开工流程，不增加“已读/准备开始/请继续”往返。

考虑换上下文的信号：阶段目标明显变化；已有假设被证据推翻；重复尝试无新增信息；旧任务细节持续干扰当前工作；需要独立检查某项结果。历史长或 token 多仅是线索，不能冒充准确的“污染程度”。

原生子智能体采用真实的受限任务包和不继承整段历史的创建方式。需要新 Desktop 上下文时使用宿主支持的新会话创建与身份绑定；发送摘要到旧会话不算清空历史，fork 全历史也不算干净起点。

主入口日常保持稳定。主 Agent 自身需要轮换时，应有真实的新会话交接和当前入口提示；不能承诺同一个物理会话 ID 在后台被清空。新主身份必须对应新配置，旧配置、锁与运行证据保留。

独立上下文不等于严格保密隔离。不得用“没有默认传入资料”证明其他任务资料绝对不可读；评测的隐藏验收另设边界。

## 7. 自动推进必须落到真实宿主调用链

**首期路径：主会话保持活跃，原生等待结果，然后推进同一状态机。**

1. 控制器发出 nextAction 和动作 ID。
2. Skill 驱动主 Agent 调用真实的派工/绑定/等待工具。
3. 等待返回完成、阻塞或变化。
4. 控制器核对身份、attempt、产物和回执。
5. 返回下一项可执行动作；主 Agent 在授权范围内继续。
6. 只有目标完成、用户暂停、预算耗尽或确需用户决定时收尾。

nextAction 沿用现有设计，补充计划修订、阶段交接等动作；每项带 actor、输入版本和幂等标识。对于外部派工先记录意图，再调用工具，再绑定真实回执。回执丢失属于待对账，不能依据超时再派一次，也不声称实现跨进程绝对 exactly-once。

自动完成后的触发必须验证；把结果写入文件不会自动唤醒一个已经结束的模型轮次。

**第二层增强：仅在兼容性验收后启用的生命周期 Hooks。**

- Stop：尚有已授权、可继续且预算足够的动作时提示继续；完成、暂停或阻塞时放行结束。按状态版本去重，限制连续接续次数，禁止无变化空转。
- SessionStart/compact：重新装载短计划锚点和状态引用，不注入全部历史，不自行伪造压缩。
- 异步 hook：可保存记录，但不能当作闲置会话唤醒机制。

官方 Hooks 文档说明了 Stop 的 continuation 和 compact 后的上下文注入，也明确异步 hook 完成不会启动闲置会话的新轮次；这些机制仍需在目标 Desktop 版本实测。[官方 Hooks](https://learn.chatgpt.com/docs/hooks)

关闭应用、宿主断线或用户中断后，首版保证状态可恢复。无人值守的离线唤醒单列为后续能力；不能用定时轮询、新终端模型或隐藏服务代替尚未验证的宿主能力。

## 8. 动态变更协议

变化可能来自用户新要求、发现新依赖、接口不兼容、测试反例、资源冲突、执行者状态变化或预算压力。

处理顺序：

**记录变化 → 比较当前计划 → 计算影响范围 → 暂缓受影响的新派工 → 提交局部修订 → 校验并生效 → 完成交接 → 继续。**

| 变化 | 默认处理 |
| --- | --- |
| 实现细节调整，目标/接口/写集不变 | 当前负责人处理，保留结果和理由 |
| 新依赖、写集需要转移、组内/跨组安排变化 | 主 Agent 局部修订计划；受影响工作安全交接 |
| 目标、对外行为、交付范围或总预算实质变化 | 主 Agent 说明影响并请用户决定 |
| 外部派工状态不明 | 先对账，不作为改路线重派的理由 |

未执行任务采用新版本；正在执行的 attempt 保持旧合同。完成后核对它的输入、对外交付接口和验收是否仍兼容新计划。兼容则复用；不兼容则标明被替代，保留成果但不能解锁新依赖。

明确取消或切换必须记录独立动作，确认原执行状态、回收归属，再派新的尝试。超时不自动释放文件所有权。

验收变更不得追溯修改已经失败的尝试。确有业务变更时形成新验收版本，保留旧结论及变更原因，防止通过改标准“修复”失败。

## 9. 工作区、写集与集成

并行读取可以重叠。写入按任务拥有明确的文件或模块范围，接口和公共配置设置一个集成负责人。顺序任务可以在前一任务交付并释放后接管相同文件。

跨 worktree 的逻辑写集以 projectId、仓库身份和仓库相对路径统一登记，不能只比较不同工作区的绝对路径。组主只派发和提交自己绑定的 run；总协调读取受控聚合事实并处理跨组交接，不伪装组主身份操作其运行。共享文件的归属转移由同一项目级控制记录原子更新。

需要并行改码时，为执行任务分配受管 worktree；执行宿主必须能够在指定目录工作。既有 Desktop 绑定与工作目录校验要真实适配，不能只换一个路径参数就声称隔离已完成。

宿主不支持独立工作目录时，明确回退为受控串行或已验证的组级工作区方案，不能让多个执行者无约束写同一主目录。

worktree 准备要包含：

- 明确基准提交；未提交工作不悄悄丢弃或自动提交。
- 锁文件对应的依赖准备与必要测试环境。
- 启动服务的端口和进程归属；就绪检查失败不派后续任务。
- 凭据和运行环境由受控配置提供，不复制用户整份环境。
- 失败现场保留，清理只针对已经确认无活跃拥有者的工作区。

依赖包缓存可复用，避免复制整套开发环境；不让并行任务共享可写的依赖目录。worktree 是代码分离，不替代 OS 权限沙箱。

集成负责人验证各任务输入和交付接口，按依赖应用改动，在确定版本上执行一次集成验收。改动、依赖或验收输入变化时才做相应复验。各任务自测不自动等于集成验收。

## 10. 状态、知识与最小代码改造

沿用 Node.js/ESM、现有 LangGraph、SQLite、Markdown 与 FTS5/BM25，不引入第二套外部编排依赖。框架核心与 Coding Agent 场景先在同一仓库内明确职责，按实际复用需要拆模块。

当前 workflow.mjs 的任务/文件归属/事件表、依赖判断和派工对账主要由控制器实现；LangGraph 连接 collect、dispatch、accept、capture 四个阶段并写图检查点。SQLite 连接目前来自 SqliteSaver。第一轮保留这些实现，核心合同不导入 LangGraph 内部类型，业务状态以控制器记录为准。图检查点不等于 Codex 会话恢复，也不保证外部派工恰好一次。若后续仍只需要这层简单连接，再以明确迁移任务替换数据库入口与阶段推进；不得为了“自研框架”先重写整套运行时。

| 当前资产 | 处理方式 |
| --- | --- |
| Skill 和 direct/native/Desktop 执行协议 | 保留；补计划版本、阶段策略和自动等待/接续 |
| contracts.mjs、workflow.mjs | 保留既有回执/身份/去重/恢复；增加阶段映射和版本校验 |
| knowledge.mjs、retrieval.mjs | 保留 BM25；补计划/接口知识的适用版本与评审状态 |
| continue-gate.mjs | 保留可观测事实提示；新增信号必须有实际记录支撑 |
| cost-report.mjs、trace-report.mjs、usage.mjs | 复用；补全根协调、组协调、交接与恢复归因 |
| desktop.mjs | 封装版本敏感能力；预检项目、身份、目录和会话状态 |
| 新分支 git-workspace、local-check、补丁/回执校验 | 按职责提取复用，适配原型身份与任务流程 |
| 新分支 repair receipt 修复 | 单独核对并保留对应回归 |
| 新分支独立模型执行产品、Web 前端、展示页 | 不作为本轮改造主线；现有草稿 PR 后续按新方案拆分 |

新增职责最初集中在计划版本与阶段推进、交接记录两个小模块，宿主调用仍通过现有 Skill/adapter；避免给每个概念各建一套服务。

数据库只补充计划版本、阶段关联、交接及变更记录；runs/tasks/events/验收记录继续复用。状态事实以 SQLite 为准，原文知识以 Markdown 为准，计划规则以已生效快照为准。

第一轮采用“一个阶段下每个组复用一个旧 run”。同一开发目标下面可先 native、再 direct、再 Desktop。阶段之间校验产物并移交文件归属。失败恢复沿原 run；新阶段才创建新 run，不能用切阶段清空失败和返修预算。

暂不在同一个旧 run 内混合全部载体。遇到确实无法用阶段组合表达的任务，再增加任务级 executor 和逐任务 nextAction；不能仅删除 validateRequest 的限制，因为派工、领取、绑定、收集和恢复也依赖 mode。

迁移采用增量 schema 版本。旧运行保留旧合同、配置和验证方式，不自动重写历史；仅新目标使用新版计划。用户配置、真实身份、原始日志、私有知识和简历资料继续不进入 Git。

## 11. 调度与上下文成本控制

根和组主的工作量计入总成本；子预算从目标总预算分配，不能每次新建组或新 attempt 又领取一份总预算。

实际并发额度覆盖调查、编码、验收等所有活跃执行者；总协调不能在已满的分组之外再额外开一批执行者。空闲但尚未关闭的原生线程可能仍占宿主额度，回收前先确认没有未完成交接；资源报告区分配置上限、可用槽位和实测活跃数。

优先限制可执行的资源：阶段/整体时间、并发、模型调用与返修次数；token 用量以实际回执记账，缺失时标为未知。运行中无法精确取得用量时，不宣称绝对 token 硬限额，先停止新的派工并保留已经在途的消耗。

每次扩容记录：就绪宽度、关键依赖、选择理由、预计收益及不确定性。完成后对照真实交付耗时和协调开销，不以启动人数或重叠秒数证明提效。

每次上下文轮换记录：触发原因、交接量、创建/装载时间、输入/缓存/输出 token，以及是否发生遗漏、重复调查或旧假设复用。冷启动可能减少冗长历史却降低缓存命中，必须一起比较。

减少重复调用的措施：

- 固定规则校验和事实聚合使用程序，不追加模型评分角色。
- 无变化状态不反复查询和生成报告。
- 有效验收复用，知识修复不重做工程。
- 主会话仅接收必要摘要；详细日志按需读取。
- 不为每个短任务重新创建主会话，不用无效派工维持人数。

## 12. 一个完整场景

样例：已有 TypeScript 工单应用新增批量导入。此处是拟议验收用例，不是已有完成记录。

| 阶段 | 工作 | 初始调度 |
| --- | --- | --- |
| P0 需求与设计 | 固定导入格式、权限、失败语义、接口和验收 | 主 Agent；必要时 1–2 个只读子任务 |
| P1 契约基础 | 数据结构、接口约定、数据库变更 | 有依赖的工作先串行 |
| P2 模块实现 | 解析、校验、批处理、导入 API、上传界面、结果展示 | 若六项在接口冻结后独立且工作量值得，候选 1-2-6 |
| P3 集成 | 合并各模块、端到端行为检查、局部返修 | 收缩为主 Agent 与必要执行者 |
| P4 交付 | 补丁、验收结果、已知限制、有效知识候选 | 主 Agent 汇总现有证据 |

P2 中发现权限规则影响 API 和界面：暂停这两项的后续派工/在安全点交接，保留其他独立任务；根更新接口和计划版本，先解决权限契约，再重新释放受影响任务。旧版本迟到结果不会自动解锁新版节点。

加入上下文场景：一个执行者反复依赖已作废字段。保留失败证据和当前代码，在阶段/尝试交接点用新上下文接手；只传当前接口、已做工作、错误事实和剩余事项，检查是否避免重复相同错误。

示例必须测量完整链路；如果六项实际高度耦合或任务太短，应按计划自动选较小规模，不能为展示 1-2-6 强行拆分。

## 13. 实施里程碑与验收

里程碑按依赖推进，暂不承诺未验证的工期。每一项有可观察结果，达到后再扩大范围。

| 里程碑 | 交付 | 验收条件 |
| --- | --- | --- |
| M0 原型与宿主能力基线 | 整理可复用代码；确认项目定位、真实新会话创建及异步完成、目录核对、身份绑定、发送/等待/读取与接续能力 | 新建和已有会话分别验证；工具权限和宿主版本明确；不绕过宿主上限 |
| M1 文档驱动与项目路由 | 轻量总览、每项目单源计划、版本冻结、阶段映射；项目内主 Agent + ≤3 子智能体 | 用户在同一总调度入口交代两个项目；项目身份、规则与状态不串用；重复请求不重复派工 |
| M2 自动交接与上下文接续 | 交接包、版本接收、原生等待推进、主上下文限量输入 | 下阶段自动接上；失败事实和用户约束不丢；真实新上下文可验证 |
| M3 动态变更 | 局部计划修订、影响范围、归属交接、旧结果兼容性 | 新依赖/接口变更/取消等场景仅影响对应分支；旧结果不误解锁 |
| M4 多项目资源与扩容 | 1-2-6、资源准备、全局与项目预算、每项目 run、跨项目交付核对 | 项目并行推进；额外执行扩容满足收益条件；能收缩；低优先级不长期饥饿；无重复拥有者 |
| M5 扩展与发布 | 经验证的 1-3-9/1-4-12 能力、评测、安装说明、真实演示 | 每一拓扑单独标验证状态；发布能力与实测一致；数据可复现 |

阶段 M0 先确认宿主可运行，再实施依赖该能力的功能。如果特定 Desktop 版本不支持创建新阶段会话或真实等待，该版本不标“自动交接已支持”；保留可用的单主路线及状态恢复，不偷偷改成另一个产品。

首个完整演示覆盖 M1–M4：开发文档→默认轻量路线→必要时 1-2-6→出现真实依赖变化→局部修订→新上下文接续→验收交付。大规模拓扑不成为首版发布的装饰性硬指标。

M1 可以用两个项目验证最小增量；首版核心演示必须从同一个总调度窗口推进第 19 节的 A/B/C 三个项目。中途改变 A 的需求、令 B 中断并恢复、让 C 在资源调整后继续，核对项目身份、计划版本与代码交付。仅在单项目内部多开窗口不能替代这一验收，不要求九个执行者同时运行。

## 14. 验证与评测

**机制回归先复用现有测试。** 优先扩展 contracts/workflow、lightweight-cli、handoff、project-binding、dispatch-recovery、blocked-task-repair、knowledge-scope、cost-report 等覆盖，不另建平行测试框架。

新增缺口重点是：计划版本竞争、阶段衔接、顺序文件接管、组预算、部分变更、迟到旧回执、动态缩容、暂停与 Stop hook 竞争。确定性替身只验证状态协议，不能证明模型能合理路由或判断上下文。

**先 12 个真实任务试点。** 4 修复、4 功能、4 重构，来自两个冻结提交的可运行样例仓库，覆盖简单任务、真实并行工作、依赖链、阶段交接与变化。样例仓库和任务需要新建，未附原始材料的历史描述不作为评测数据。三项目联动案例另用于验证协调机制，不与这组任务混算分母。

主对照使用当前原型与升级方案，双方获得相同需求、起始代码、可见知识、模型配置、工具和总资源上限；原型允许使用已有原生能力，不人为强制全部串行。前期计划生成、注册/设置耗时分别记录，并计入相应端到端口径。

第一轮每题每方案各 1 次，共 24 次，是探索性试跑。必要时针对明确假设追加少量消融，如固定载体与自适应载体、有无阶段交接；不立即展开昂贵全矩阵。原生 Codex 作为额外产品对照后续另设组，不能把不同条件的数据混为一种基线。

稳定后扩展 24 修复 + 24 功能 + 12 重构。每方案每题 3 次，共每方案 180 次；两个方案合计 360 次。若增加第三组，单独增加预算。每次运行仅取预先规定的最终提交，保留全部失败和超时。

**20 项故障题建议分为五组，每组四项：**

1. 进程/阶段/验收/主会话中断。
2. 重复请求、派工回执丢失、迟到结果、错误 attempt。
3. 文件冲突、端口占用、依赖缺失、工作区基线漂移。
4. 计划版本冲突、接口变化、验收版本变化、暂停与继续竞争。
5. 交接缺少约束、过期知识、错误项目来源、重复失败未保留。

先用确定性故障注入检查协议，再挑必要场景用真实宿主和模型验证。

**指标和口径：**

- 正确性：首次正式验收通过率；返修后最终通过率另列。
- 时间：从用户任务进入到代码与验收可审查的总墙钟时间，包含规划、等待、协调、返修；启动设置单独列明。失败按预注册口径封顶或删失，并同步报告成功率。
- 成本：根协调、组主、执行者、规划、交接、恢复、验收与日志处理全部计入；缓存输入是输入子集，推理是输出子集，不重复相加，不将 token 直接冒充账单。
- 人工：介入次数与耗时、用户传话次数、人工处理冲突次数。
- 重复工作：已有有效结果被重复执行/调查/改写的次数，结合事件与人工复核；不能只用代码行数代替。
- 上下文代理指标：交接漏约束、过期假设被使用、错误项目知识、重复已否定尝试；这些不是完整的“污染率”。
- 调度：规划人数与实际并发、关键路径等待、扩缩容次数、协调时间及总耗时关系。

多项目专项增加：目标路由准确性、相似文件名/接口名造成的跨项目误用、A 项目变更对 B 项目的错误影响、优先级调整后的等待与资源公平、总调度输入中的执行噪声、项目总览版本滞后。能力强弱与上下文策略分开研究；不以“换了更强模型”单独证明隔离有效。

实际模型无法观测时标为未知，只记录请求配置，不能用模型自述补齐证据。

隐藏验收在生成 Agent 不可访问的独立环境运行；不同方案隔离可访问的任务历史和知识回写。当前 Desktop 的历史跨任务读取反例不能被普通文件夹隔离或 Skill 宣言消除；隔离未成立的组仅作功能诊断，不能进入盲测排名。

没有可核对原始记录的历史指标不进入公开结果。重新运行后发布实际数据和不确定性，不预填成功率或提速比例。

**功能版本发布闸门：** 机制不变量全部通过；无已发现的越界交付、重复执行误判或错误验收放行；所有尝试可追溯；三项目真实案例可复跑；安装与能力范围可被另一位开发者核对。源码和开发计划可以先按原型状态开源，不能因此宣布新版交付完成。提效宣传单独等待公平对照，不将大规模评测或 1-4-12 作为原型公开的前提。

## 15. 开源交付与首屏展示

分发包以现有 Skill + Node 控制器为基础，安装诊断检查 Node/Git、项目登记、实际宿主能力、可用工具和配置。保留本地私有配置。第一批明确标注验证过的 Desktop/OS 版本，不能由跨平台单元测试推导全平台桌面兼容。

GitHub 首屏展示：

- 一句话说明用户收益：在 Codex 里提出目标，系统按开发计划组织协作，并在变化和交接中保持任务连续。
- 一段真实录像：从计划到分工、局部调整、上下文交接、最终代码与验收。
- 一个可重复运行的完整样例，附冻结提交、步骤、限制和原始记录索引。
- 架构与关键设计取舍：为何最多 3 个原生执行者、何时分组、如何防止旧上下文和旧计划继续生效。
- 诚实的功能/验证矩阵与评测报告。

仓库维护补齐许可证核对、贡献指南、支持范围、故障报告模板和 CI。普通 CI 使用确定性测试，不默认调用付费模型；真实模型评测通过明确入口和预算单独执行。

现有草稿 PR #1 不整体合并。后续实现以原型为基础，分里程碑提交；从实验分支择取已经有用途的修复和工具。前端素材和 headless 产品入口不混进本次核心改造。发布和开发进度明确区分“源码已公开”“某里程碑已验证”和“提效得到对照支持”。

## 16. 依据与未决事项

本地原型证据（以上述原始提交为准）：

- src/contracts.mjs：run 级 mode、依赖限制、任务数量与并发耦合、写集校验。
- src/workflow.mjs：冻结任务、真实身份绑定、nextAction、派工意图/回执、恢复与验收。
- skills/codex-project-workbench/references/handoff.md：短交接、真实新 PM、历史配置与证据保留。
- docs/stage-handoff-results-20260912.md：有真实阶段交接观察；任务不同，不能作因果提效；冷启动的非缓存成本可能增加。
- docs/dispatch-scale-results-20260912.md：历史 6 人编码重叠与 9 人知识交付重叠范围不同；不证明 12 人可用或大并发更快。
- docs/normal-prereview-results-20260912.md：实际协调成本仍需减少。
- docs/desktop-hooks-results-20260911.md：历史 PreToolUse 故障反例，不是生命周期接续验证。
- docs/desktop-tool-authorization-assessment-20260911.md：版本限定的宿主工具授权边界。

官方文档核对于 2026-09-21：

- [Subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)：原生委派、上下文分担与协调开销。
- [Configuration Reference](https://learn.chatgpt.com/docs/config-file/config-reference)：agents.max_concurrent_threads_per_session；实际支持要按安装版本核对。
- [Hooks](https://learn.chatgpt.com/docs/hooks)：Stop continuation、compact 上下文恢复、异步 hook 不启动闲置轮次。

首期必须消除的未决项：当前目标 Desktop 版本的创建/等待/新上下文能力、跨组实际并发额度、worktree 绑定方式和生命周期 hook 可用性。这些通过 M0 的小范围真实能力验收解决。原始业务评测材料不假定存在；样例与任务集按本方案新建。

## 17. 开发任务与领工顺序

下表是实现顺序和默认写集。新测试文件名是交付要求，当前并不存在；未实现前不能声称对应命令通过。共用 workflow.mjs、contracts.mjs、cli.mjs 的任务串行集成。一个集成负责人维护代码与验收，最多派出三个互不覆盖写集的有界子任务，不为满足人数重复审计或规划。

| ID / 依赖 | 输入与应交付结果 | 默认写集 | 验证入口与失败条件 |
| --- | --- | --- | --- |
| WB-00 / 无 | 核对原型、依赖、宿主工具与权限；交付有日期和版本的 capabilities 矩阵，区分已有会话、新建会话、等待、恢复、工作目录、模型配置及实际观测 | docs/host-capabilities.md；必要时 src/desktop.mjs 与 test/desktop.test.mjs；docs/development-status.md | 先跑 npm test 基线；针对适配改动跑 node --test test/desktop.test.mjs test/project-binding.test.mjs；创建能力需真实回执，工具不可用/没有授权标受限，不以 mock 标已支持 |
| WB-01 / WB-00 | 定义项目、计划版本、任务、attempt、nextAction 与回执的最小合同；现有合同继续可读 | src/contracts.mjs；test/plan-contract.test.mjs；examples/plan.example.json | node --test test/plan-contract.test.mjs test/project-binding.test.mjs；错误项目、版本缺失、循环依赖、未知执行载体必须被拒绝 |
| WB-02 / WB-01 | 解析一份 Markdown 计划块，生成不可变快照；以 expectedRevision 原子激活，保留旧 attempt | src/plan.mjs；src/workflow.mjs 的 schema 迁移；test/plan-store.test.mjs | node --test test/plan-store.test.mjs；并发修改不能双成功，半写快照不能变为 active；旧数据库可打开 |
| WB-03 / WB-02 | 显式项目登记、总览与项目计划引用、独立知识范围、增量状态；先跑通两个项目路由 | src/portfolio.mjs；src/cli.mjs；test/portfolio-routing.test.mjs；examples/portfolio.example.json | node --test test/portfolio-routing.test.mjs test/knowledge-scope.test.mjs；相同 taskId/文件名不得跨项目路由或读取错误知识 |
| WB-04 / WB-02、WB-03 | 按现有 run 组合阶段，沿用 direct/native/Desktop 执行；模型提出三项决策，控制器核对依赖/身份/能力/写集；完成后自动取下一动作 | src/workflow.mjs；src/cli.mjs；test/phase-execution.test.mjs；skills/codex-project-workbench/references/execution.md | node --test test/phase-execution.test.mjs test/lightweight-cli.test.mjs test/dispatch-recovery.test.mjs；不得删除旧约束后直接启用混合 mode，不得重复派工 |
| WB-05 / WB-04 | 有版本交接包、输入校验、真实新上下文接收、迟到确认对账；阶段推进不要求用户重复说继续 | src/handoff.mjs；src/workflow.mjs；test/context-handoff.test.mjs；skills/codex-project-workbench/references/handoff.md | node --test test/context-handoff.test.mjs test/handoff.test.mjs；缺约束、错基线、丢失败事实、把旧会话摘要当新上下文均失败；宿主链另做真实验证 |
| WB-06 / WB-05 | 局部修订、受影响任务集合、冻结在途合同、旧结果兼容判定、顺序文件归属转移 | src/plan.mjs；src/workflow.mjs；test/plan-change.test.mjs | node --test test/plan-change.test.mjs test/blocked-task-repair.test.mjs；无关项目不得失效，旧结果不得误解锁新版依赖，取消未证实不得释放写集 |
| WB-07 / WB-03、WB-04 | 全局额度原子分配与归还、负责人代次、单项目/全局暂停、公平分配、阶段扩缩容；默认最小规模 | src/portfolio.mjs；src/workflow.mjs；test/portfolio-budget.test.mjs | node --test test/portfolio-budget.test.mjs test/portfolio-barrier.test.mjs；并发申请不得超额，旧负责人不得派工，回执不明不得释放额度，独立项目不得被无谓屏障阻塞 |
| WB-08 / WB-01 可先做夹具；正式运行依赖 WB-05–07 | 建立第 19 节三个自包含仓库夹具、冻结基线、独立验收与故障脚本；完成首次真实三项目案例及失败恢复验证，记录完整结果索引 | examples/three-projects/；scripts/verify-three-projects.mjs；test/three-projects.test.mjs；docs/three-project-demo.md | node --test test/three-projects.test.mjs 验证确定性协议；node scripts/verify-three-projects.mjs 只核验已保存证据，不自动调用付费模型；真实会话运行单列步骤和费用边界 |
| WB-09 / WB-08 | 复用三项目案例证据做成本归因；设计并冻结 12 题探索性对照协议，按明确预算运行，避免为写报告重跑成功案例 | docs/evaluation/；scripts/ 下最小必要评测入口；现有 cost-report/trace-report 对应测试 | npm test；检查每次运行的请求/实际模型、预算、输入与最终提交、失败记录、计数分母；没有真实对照不发布提效结论 |
| WB-10 / WB-08 核心通过；WB-09 结果按实际范围披露 | 普通用户安装与绑定说明、支持矩阵、公开样例、贡献入口、确定性 CI、版本说明 | README.md；CONTRIBUTING.md；scripts/install.ps1（仅实际缺口）；.github/workflows/；package 元数据 | 干净克隆安装与 npm test；CI 不调用付费模型；源码公开可以先完成，功能版本需满足第 20 节；不得把历史草稿 PR 整体合入 |

开始实现前可根据实际代码合并相邻小任务，但要记录原因，保留各项验收义务。除 WB-08 夹具等独立工作外，不在前置合同未确定时平行改相同核心文件。修复已有问题优先复用已有测试，测试验证不变量和反例，不只是复制实现。

每次提交在 docs/development-status.md 记录：任务 ID、变更范围、确切提交、验证命令/时间/结果、真实宿主证据或未验证项、下一可执行任务。公开摘要不含私有路径、真实会话 UUID、原始对话或个人凭据。原始证据保留本机，公开材料使用一致的代号和可核对的代码/产物哈希。

## 18. 最小协议示例与状态一致性

以下 JSON 是拟议合同示例，用于 WB-01 固化和测试；不是当前可直接执行的 CLI 输入。路径引用由私有项目登记解析。模型不能凭文档中的 shell 文本获得执行授权。

**总览：只维护项目引用与资源。**

```json
{
  "schemaVersion": 1,
  "portfolioId": "demo-portfolio",
  "revision": 1,
  "coordinatorEpoch": 1,
  "budget": {"maxActiveWorkers": 3, "maxAttemptsPerTask": 2},
  "projects": [
    {"projectId": "project-a", "planId": "bulk-import", "acceptedPlanRevision": 1, "priority": 3},
    {"projectId": "project-b", "planId": "sdk-compatibility", "acceptedPlanRevision": 1, "priority": 2},
    {"projectId": "project-c", "planId": "search-order", "acceptedPlanRevision": 1, "priority": 1}
  ],
  "dependencies": []
}
```

**项目计划：单源规则与可执行快照。**

```json
{
  "schemaVersion": 1,
  "projectId": "project-a",
  "planId": "bulk-import",
  "revision": 1,
  "objective": "按已确认接口实现批量导入并通过冻结验收",
  "phases": [{"id": "implement", "dependsOn": [], "maxNativeWorkers": 3}],
  "tasks": [{
    "id": "validate-input",
    "revision": 1,
    "phaseId": "implement",
    "dependsOn": [],
    "executor": "native",
    "files": ["src/import.mjs"],
    "contextRefs": ["import-contract-v1"],
    "acceptanceRefs": ["import-acceptance-v1"]
  }]
}
```

**动作和回执：程序绑定版本、身份与证据。**

```json
{
  "nextAction": {
    "id": "action-001",
    "type": "DISPATCH",
    "portfolioId": "demo-portfolio",
    "projectId": "project-a",
    "coordinatorEpoch": 1,
    "planRevision": 1,
    "taskId": "validate-input",
    "taskRevision": 1,
    "attemptId": "attempt-001",
    "actorBindingRef": "project-a-lead",
    "packetHash": "sha256:<generated-from-frozen-packet>"
  },
  "receipt": {
    "actionId": "action-001",
    "projectId": "project-a",
    "attemptId": "attempt-001",
    "status": "ACCEPTED",
    "hostReceiptRef": "local-private-evidence:dispatch-001"
  }
}
```

哈希占位和 evidence ref 必须在真实运行时由程序生成；真实身份来自宿主回执并存于本机。上例 ACCEPTED 仅表示派工被接收，不能表示代码完成或验收通过。完成回执还需绑定任务包、输入和产物哈希，验收回执单独记录。

**跨项目版本规则。** 每个项目计划的 active revision 是本项目执行依据；总览保存已观察的 acceptedPlanRevision 和最后事件序号。项目发布修订时追加事件，总调度按序消费并更新引用。引用落后显示 STALE_OVERVIEW，不能拿旧总览覆盖项目新计划。跨项目依赖绑定具体交付物版本、来源与哈希，经兼容性核验才解锁；仅看到“项目已完成”不够。首版同机控制库和事务即可，不引入分布式事务平台。

**额度与负责人规则。** 额度预留在全局控制库的事务中完成，至少绑定 portfolio/project/run/attempt/owner epoch；同一动作重复申请返回原预留。记录宿主的活跃/已打开会话限制，区分负责人会话与工作执行者，分别满足宿主约束。确认完成、取消或对账证明未派出后归还；送达不明时保留预留。负责人交接增加 epoch，旧 epoch 的新动作被拒绝；此约束不能冒充已强制终止旧会话。预算是用户授权范围，模型不得自行扩张。

| 状态或错误 | 要求 |
| --- | --- |
| PLAN_CONFLICT | expectedRevision 不匹配，拒绝激活；读取当前版后局部合并 |
| STALE_OVERVIEW | 继续保留项目真实状态；刷新总览，核对跨项目输入后再派工 |
| DISPATCH_UNKNOWN | 保留意图、写集和额度；读取宿主证据对账，不按超时重发 |
| INPUT_STALE | 输入代码、知识或接口版本变化；拒绝自动接收并记录影响范围 |
| RESULT_SUPERSEDED | 保存旧 attempt 的结果；不解锁不兼容的新任务 |
| PROJECT_PAUSED | 该项目停止新派工；其他独立项目继续；运行中任务状态如实显示 |
| PORTFOLIO_PAUSED | 所有项目停止新派工；Stop hook 和旧 epoch 不能继续派发 |
| CAPABILITY_UNAVAILABLE | 显示支持级别和缺口；不得伪造新会话或暗换 headless 执行器 |

新增错误码是协议类别，允许沿用现有错误包装；不能为了命名统一而丢失既有失败证据。崩溃恢复先读取控制器事务状态和派工记录，再核对宿主与图状态。即使图节点重放，也不能重新发送已经存在且尚未完成对账的动作。

## 19. 首个三项目真实验收

先提交可复跑的合成业务夹具，再在临时目录初始化三个独立 Git 仓库，冻结各自 base commit。夹具源码属于本仓库；运行中的嵌套 .git、依赖缓存、真实身份与日志不得提交。三个项目使用同名 src/config.mjs 和独有断言，主动检测路由混淆。

| 项目 | 固定目标 | 独立验收 |
| --- | --- | --- |
| A：工单导入模块 | 增加批量导入校验，错误行返回稳定错误码；中途增加租户字段约束，形成计划 v2 | 行级错误、重复条目、租户隔离断言；node --test；旧接口结果不能满足新验收 |
| B：SDK 兼容模块 | 修复一个旧调用方式的兼容问题；开发中断后用交接包接续 | 新旧调用输出、错误处理与已有回归；node --test；恢复不得重做已经有效的交付 |
| C：检索排序模块 | 修复同分结果顺序不稳定，并保持项目知识范围 | 确定排序、同名资料的项目范围与已有回归；node --test；低优先级仍能获得执行机会 |

夹具验收由集成负责人预先固定，生成执行者不得修改已冻结验收脚本；需要业务调整时由负责人创建新验收版本并保留旧结论。若验收在同一账户可读，只标为功能验收，不能称隐藏盲测。

真实流程：

1. 从一个总调度入口提交三个目标，登记三个独立项目上下文，记录各自真实身份、目录、基线和明确模型配置。创建额外用户任务必须符合宿主授权规则；手动创建后绑定与自动创建分别记录支持状态。
2. 主 Agent 给出各阶段调度理由，控制器检查实际容量。先用低并发证明正确，不凑满 1-3-9；每个项目负责人可以直接实现自己的小任务。
3. 注入 A 的租户规则变更；只暂停受影响任务，生成新版计划和交接，检查 B/C 合同及产物没有被错误修改。
4. 在 B 的已记录安全位置中断，保存失败/未完事实；核对状态后在真实新上下文接续。不能用 fork 全历史冒充干净上下文。
5. 暂停 A，调整 C 优先级；检查 B/C 继续、额度不超限。随后全局暂停，确认没有新派工；恢复只能来自有效授权和当前负责人。
6. 对三个仓库分别验收，记录最终提交、全部尝试、失败与返修。主入口只汇总结果、关键决定和可按需检查的证据。

通过条件：三个项目都取得可审查代码进展并完成冻结验收；无错项目派工/验收、无未经核对的重复派工、无旧版本误放行；暂停和局部变更的影响范围正确；交接保留用户约束与失败事实。若宿主能力缺失，报告哪一步未验证，不以协议 mock 代替真实链路。

公开记录至少包含运行日期、宿主/工具版本、请求模型与可观测的实际模型、仓库基线/最终提交、计划版本、脱敏事件序列、验收结果、协调与执行耗时、全部角色用量及缺失项。真实录像是后续展示材料，不能用模拟界面代替产品运行证据。

## 20. 开源与接续执行要求

**源码公开。** 原创代码采用根目录 MIT 许可证，第三方仍遵守其保留声明。GitHub 公开前检查当前文件、远程分支和提交历史；忽略文件不是可提交材料。源码公开和开发计划发布不要求先完成尚未实现的功能。package.json 的 private:true 暂留，防止误发布 npm 包；这不影响 GitHub 开源。

**实现推进。** 由新的独立开发任务从已发布 main 建立自己的隔离 checkout，使用用户明确要求的模型与推理强度；具体会话 ID、路径与配置保存在本机交接，不能写入公共规则。不要继承原对话全文。现有原型 checkout、个人 Codex 配置和其他项目任务保持独立；安装或替换个人全局 Skill 需要相应授权，优先在测试配置验证。

**GitHub 交付。** 新任务按 WB 里程碑提交、推送实现分支并创建 PR；每次 PR 说明问题、行为变化、实际验证和未验证边界。持续更新开发状态，完成最终功能版本的发布检查。不要 force-push 主分支、删除历史或整体合并 PR #1；需要复用其中修复时按文件和回归单独审查。不得把 commit、PR 或 CI 通过说成真实三项目验收已经通过。

**成本边界。** 正常实现和必要的小范围真实能力验证按现有授权推进；先形成可运行最小增量，不把 360 次模型评测作为开工条件。批量真实评测必须先记录总调用/时间/用量预算，预算未明确时完成脚本和确定性验证，不能自行无限追加模型调用。宿主不允许自动创建额外用户任务时，保留可继续的开发工作，说明具体限制和所需最小用户操作。

**结束条件。** 每次交接必须给出已经完成的任务、确切提交和验证证据、未解决问题、下一可执行任务。首个功能版本完成 WB-00–08 的核心义务和 WB-10 的安装/发布要求；WB-09 的比较结论只按实际完成范围发布。后续更大拓扑和任务集持续作为有状态的开发目标，不能伪装成当前已支持功能。
